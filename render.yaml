services:
  - type: web
    name: robo-bolsonaro-deceint-discord-bot
    env: python
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: python app.py
