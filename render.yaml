services:
  - type: worker
    name: robo-bolsonaro-deceint-discord-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: chmod +x ffmpeg && python app.py
